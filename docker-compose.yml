version: '3.8'

services:
  minikernel-simulator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: minikernel_simulation_core
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
